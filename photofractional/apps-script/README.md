# PhotoFractional Apps Script

Deploy this as a **separate** Google Apps Script web app from the pigmentation and rosacea landings.

1. Create a new Apps Script project and paste `Code.gs`.
2. Deploy as a web app (Execute as: Me, Access: Anyone).
3. Paste the `/exec` URL into `photofractional/index.html` as `APPS_SCRIPT_URL`.

Email subjects use `[RHS PhotoFractional Lead]` / `[RHS PhotoFractional Click]`.
Notifications go to `damnart.seo@gmail.com`.
